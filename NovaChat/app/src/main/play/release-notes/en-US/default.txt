• Message delete now works correctly
• Message action menu: close button no longer cut off on small screens
• Block dialog shows only options that apply (e.g. block by number only when a number is available)
